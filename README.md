Run `npm install` and then `bower install` to grab all dependencies.
Then run `gulp` to start the front-end on [localhost](http://localhost:3000). It will automatically open the application in your default browser.
